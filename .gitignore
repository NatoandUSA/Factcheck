node_modules/
server/node_modules/
.env
dist/
.DS_Store
server/app.db
server/app.db-wal
server/app.db-shm
server/*.bak*
Mautheutest/
*.xlsx
!tests/fixtures/*.xlsx
!public/templates/*.xlsx

# Mutable runtime imports are state, not source. Production must point
# OMNI_IMPORTS_DIR outside the Git worktree; dev creates this directory as needed.
data/imports/

scratch/

# Review receipts and agent scratch artifacts may contain private production
# topology or database metadata. Keep them outside every public Git tree.
/reports/

# This repository is one npm workspace. The root package-lock.json is the only
# dependency authority used by CI and production deployment.
/server/package-lock.json
